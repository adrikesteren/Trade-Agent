/**
 * Class for outline-style action links inside list-view chrome:
 * - top-right "New / Import"
 * - row-level actions
 * - "View all" links on related lists.
 *
 * Apply to `<Link>` / `<a>` / `<button>` directly.
 */
export const listViewOutlineActionClass = "bk-listview-outline-action";
