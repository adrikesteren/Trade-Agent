/** Labels for QStash managed schedules (not necessarily `automation.sync_runs.job_key`). */

export const OPS_JOB_RISK_DAILY_RESET = "risk_daily_reset" as const;
export const OPS_JOB_BITVAVO_RECONCILE = "bitvavo_reconcile" as const;
