# Put this file at the repo root (recommended). Next.js is configured with `envDir`
# pointing at the monorepo root, so `apps/web` loads these values automatically.
# You can still use apps/web/.env.local for overrides. Never commit real keys.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server only — workers / API routes (bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Bitvavo (optional for public candle ingest)
BITVAVO_API_KEY=
BITVAVO_API_SECRET=

# Upstash Redis
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Upstash QStash — verify incoming worker webhooks
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=
# Optional: publish schedules from a secure backend (required to chain multi-chunk EUR candle sweeps)
QSTASH_TOKEN=

# Public origin of this Next app (used when the worker enqueues the next candle chunk to QStash)
APP_BASE_URL=

# Optional: Vercel Cron sends Authorization: Bearer … when this env is set (see Vercel cron docs)
CRON_SECRET=

# Local only: skip QStash signature verification (never enable in production)
ALLOW_INSECURE_QSTASH=

# After EUR market sync: max markets per request to seed 5m OHLCV when none exist yet (0 = skip)
BITVAVO_MARKET_SYNC_BACKFILL_MAX_MARKETS=

# Worker: max Bitvavo candle chunks per POST before queueing the rest via QStash (default 8)
BITVAVO_CANDLES_SYNC_MAX_CHUNKS_PER_RUN=
